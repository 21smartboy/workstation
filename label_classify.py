def classify(target_lab,subset_label,all_label)
    last_lab=[]
    for item in all_label:
        if item in subset_label
            last_lab.append(target_lab)
        else:
            last_lab.append(item)
    return last_lab
